def insertion_sort(lst):
    n=len(lst)
    for i in range(1,n):
        temp=lst[i]
        j=i-1;
        for j in range(i-1,-2,-1):
            if lst[j]>temp:
                lst[j+1]=lst[j]
            else:
                break
        lst[j+1]=temp
    return lst
        
