def generate_inverted_pyramid(n):
    p = []  # Initialize an empty list

    # Loop to create each row of the inverted pyramid
    for i in range(n, 0, -1):
        p.append(' ' * (n - i) + '*' * (2 * i - 1) + ' ' * (n - i))

    return p
