def move_zeroes(nums):
    s=0 
    
    for i in range(len(nums)):
        if nums[i]!=0:
            nums[s]=nums[i]
            s+=1

    
    while s<len(nums):
        nums[s]=0
        s+=1 
    return nums
