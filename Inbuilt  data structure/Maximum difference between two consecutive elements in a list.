def max_consecutive_difference(lst):
    if len(lst)<2:
        return 0
    max=0
    for i in range(len(lst)-1):
        diff=abs(lst[i]-lst[i+1])
        if diff>max:
            max=diff
    return max

